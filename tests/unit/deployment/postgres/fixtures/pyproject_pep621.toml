[project]
name = "example-temporal-app"
version = "0.1.0"
description = "Example Temporal application"
requires-python = ">=3.10"

dependencies = [
    "temporalio==1.22.0",
    "pydantic>=2.0.0",
    "httpx>=0.25.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "ruff>=0.1.0",
]
